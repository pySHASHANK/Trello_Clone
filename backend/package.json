{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "build": "npx prisma generate && npx prisma db push",
    "start": "node server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.x",
    "cors": "^2.x",
    "dotenv": "^16.6.1",
    "express": "^4.x",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.x",
    "prisma": "^5.x"
  }
}
